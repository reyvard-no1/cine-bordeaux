<div class="max-w-4xl mx-auto">
  <!-- Breadcrumb -->
  <div class="mb-6">
    <%= link_to "← Tous les cinémas", cinemas_path, class: "text-yellow-600 hover:text-yellow-700" %>
  </div>

  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <!-- En-tête -->
    <div class="bg-gradient-to-r from-yellow-500 to-yellow-600 p-8 text-white">
      <h1 class="text-4xl font-bold mb-2"><%= @cinema.name %></h1>
      <p class="text-yellow-100 text-lg">
        <%= @cinema.city %> - <%= @cinema.postal_code %>
      </p>
    </div>

    <!-- Contenu -->
    <div class="p-8">
      <!-- Description -->
      <div class="mb-8">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">À propos</h2>
        <p class="text-gray-700 leading-relaxed">
          <%= @cinema.description %>
        </p>
      </div>

      <!-- Informations pratiques -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
        <!-- Adresse -->
        <div class="bg-gray-50 rounded-lg p-6">
          <h3 class="font-bold text-gray-900 mb-3 flex items-center gap-2">
            <span class="text-2xl">📍</span>
            Adresse
          </h3>
          <p class="text-gray-700">
            <%= @cinema.address %><br>
            <%= @cinema.postal_code %> <%= @cinema.city %>
          </p>
          <% if @cinema.latitude && @cinema.longitude %>
            <a href="https://www.google.com/maps/search/?api=1&query=<%= @cinema.latitude %>,<%= @cinema.longitude %>"
               target="_blank"
               class="inline-block mt-3 text-yellow-600 hover:text-yellow-700 font-semibold">
              Voir sur Google Maps →
            </a>
          <% end %>
        </div>

        <!-- Contact -->
        <div class="bg-gray-50 rounded-lg p-6">
          <h3 class="font-bold text-gray-900 mb-3 flex items-center gap-2">
            <span class="text-2xl">📞</span>
            Contact
          </h3>
          <% if @cinema.phone.present? %>
            <p class="text-gray-700 mb-2">
              <strong>Téléphone :</strong><br>
              <%= @cinema.phone %>
            </p>
          <% end %>
          <% if @cinema.website.present? %>
            <a href="<%= @cinema.website %>"
               target="_blank"
               class="inline-block mt-2 text-yellow-600 hover:text-yellow-700 font-semibold">
              Site web →
            </a>
          <% end %>
        </div>
      </div>

      <!-- Call to action -->
      <div class="bg-yellow-50 border-2 border-yellow-200 rounded-lg p-6 text-center">
        <p class="text-gray-700 mb-4">
          Envie de voir un film dans ce cinéma ? 🎬
        </p>
        <%= link_to "Découvrir les films", root_path,
            class: "inline-block bg-yellow-500 hover:bg-yellow-600 text-white px-6 py-3 rounded-lg font-semibold transition" %>
      </div>
    </div>
  </div>
</div>
